package FileSystem.example.FileManagementSystem_Spring.Utils;

public class FactoryUtils {
    public static long GenerateRandomSize(){
        return (long)(Math.random()*Long. MAX_VALUE)+1;
    }
}
